# psritej.com

## Brand assets
- `/assets/logo.svg` — primary favicon/logo reference for all pages.
- `/assets/og-image.svg` — shared Open Graph/Twitter preview image.

The icon-only SVG (`logo-icon.svg`) was removed to avoid confusion; all references should use `logo.svg`.
